/*
📝 This JavaScript file serves as a demonstration of basic variable types in JavaScript.
It includes examples and explanations of various variable types such as strings, numbers, booleans, arrays, and objects.
*/

// Strings
let greeting = "Hello, world!";
console.log(greeting);

// Numbers
let age = 25;
console.log("Age:", age);

// Booleans (true, false)
let isStudent = true;
console.log("Is student?", isStudent);

// Arrays
let fruits = ["apple", "banana", "orange"];
console.log("Fruits array:", fruits);

// Objects
let person = {
  name: "John",
  age: 30,
  isMarried: false,
};
console.log("Person object:", person);

// 🎯 Create a variable called favoriteColor and assign the color green to it.
let favoriteColor = "green";

// 🎵 Create a variable called songTitle and assign the song Living Rooms to it.
let songTitle = "Living Rooms";

// 🎸 Create an array called hobbies and populate it with the following hobbies: hikes, biking, swimming.
let hobbies = ["hikes", "biking", "swimming"];

// 📚 Create an object called pet and give it properties like name (eddie), species(dog), and age(15).
let pet = {
  name: "eddie",
  species: "dog",
  age: 15,
};

// 💡 Create a variable called isSunny and assign a boolean value false.
let isSunny = false;

// 🌱 Create an array called shoppingList and populate it with items: bread, rice, vegetables, ham.
let shoppingList = ["bread", "rice", "vegetables", "ham"];

// 🚀 Create an object called car and give it properties like make (ford), model (mustang), and year (1967).
let car = {
  make: "ford",
  model: "mustang",
  year: 1967,
};

// 🎨 Create a variable called favoritePainting and assign the title of your favorite painting to it.
let favoritePainting = "Nincs ilyen";
